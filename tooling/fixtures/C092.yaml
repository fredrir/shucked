legacy_code: SH-220
legacy_name: if-dollar-command
new_category: Correctness
new_code: C092
runtime_kind: ast
shellcheck_code: SC2091
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: The output of a command substitution is executed as a command.
rationale: "Remove the `$( )` wrapper and run the command directly so its exit status is tested."
safe_fix: false
fix_description: "Remove the surrounding `$(` and `)` from the reported command substitution."
source:
  shell_checks_rule: rules/SH-220.yaml
  shell_checks_example: examples/220.sh
examples:
  - kind: invalid
    source: shell-checks/examples/220.sh
    code: |
      #!/bin/bash
      $(python3 -c 'import sys' 2>/dev/null)
