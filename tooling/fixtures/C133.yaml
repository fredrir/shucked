legacy_code: SH-311
legacy_name: array-to-string-conversion
new_category: Correctness
new_code: C133
runtime_kind: ast
shellcheck_code: SC2178
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: A variable name that was already array-like is later rebound as a plain scalar.
rationale: Reusing one name for both array and scalar semantics makes later expansions harder to reason about.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-311.yaml
  shell_checks_example: examples/311.sh
examples:
  - kind: invalid
    source: shell-checks/examples/311.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2179,2128
      exts=("txt" "pdf" "doc")
      exts="${exts[*]}"
      exts+=" ${exts^^}"
      echo "$exts"
