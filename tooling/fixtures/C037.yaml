legacy_code: SH-110
legacy_name: broken-test-parse
new_category: Correctness
new_code: C037
runtime_kind: ast
shellcheck_code: SC1072
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: "A malformed `[` test expression leaves ShellCheck at a parser recovery boundary."
rationale: Correct the test syntax before relying on any other static checks.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-110.yaml
  shell_checks_example: examples/110.sh
examples:
  - kind: invalid
    source: shell-checks/examples/110.sh
    code: |
      #!/bin/sh
      # shellcheck disable=1009,1073
      if [ x = y; then :; fi
