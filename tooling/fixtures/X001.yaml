legacy_code: SH-006
legacy_name: double-bracket-in-sh
new_category: Portability
new_code: X001
runtime_kind: ast
shellcheck_code: SC3010
shellcheck_level: warning
shells:
  - sh
  - dash
description: The `[[ ... ]]` conditional form is not portable to `/bin/sh`.
rationale: Use portable `[` or `test`, or switch the script to a shell that supports `[[ ... ]]`.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-006.yaml
  shell_checks_example: examples/006.sh
examples:
  - kind: invalid
    source: shell-checks/examples/006.sh
    code: |
      #!/bin/sh
      if [[ -n $1 ]]; then :; fi
