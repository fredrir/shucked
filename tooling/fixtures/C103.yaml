legacy_code: SH-237
legacy_name: find-or-without-grouping
new_category: Correctness
new_code: C103
runtime_kind: ast
shellcheck_code: SC2146
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: "A `find` command uses `-o` without grouping the alternatives."
rationale: "Add parentheses around the `-o` alternatives in `find`."
safe_fix: true
fix_description: "Wrap the action-bearing `find` branch in escaped parentheses to make the existing precedence explicit."
source:
  shell_checks_rule: rules/SH-237.yaml
  shell_checks_example: examples/237.sh
examples:
  - kind: invalid
    source: shell-checks/examples/237.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2154,2086
      find $PKG -name perllocal.pod -o -name ".packlist" -exec rm -f {} \;
