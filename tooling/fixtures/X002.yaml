legacy_code: SH-007
legacy_name: test-equality-operator
new_category: Portability
new_code: X002
runtime_kind: ast
shellcheck_code: SC3014
shellcheck_level: warning
shells:
  - sh
  - dash
description: The `==` comparison operator inside `[` is not portable in POSIX shells.
rationale: Use `=` when writing tests for `/bin/sh`.
safe_fix: true
fix_description: "Replace each reported `==` comparison operator with `=`."
source:
  shell_checks_rule: rules/SH-007.yaml
  shell_checks_example: examples/007.sh
examples:
  - kind: invalid
    source: shell-checks/examples/007.sh
    code: |
      #!/bin/sh
      [ "$1" == foo ]
