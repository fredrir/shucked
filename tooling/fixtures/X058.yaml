legacy_code: SH-265
legacy_name: greater-than-in-double-bracket
new_category: Portability
new_code: X058
runtime_kind: ast
shellcheck_code: SC3012
shellcheck_level: warning
shells:
  - sh
description: "A `<` or `>` operator is used inside `[[ ]]` in a POSIX sh script."
rationale: "Use `-lt`/`-gt` for numeric comparisons, or another portable form for string ordering."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-265.yaml
  shell_checks_example: examples/265.sh
examples:
  - kind: invalid
    source: shell-checks/examples/265.sh
    code: |
      #!/bin/sh
      # shellcheck disable=3010,2154,2071
      if [[ "$BASH_VERSION" < 4 ]]; then echo old; fi
