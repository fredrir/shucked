legacy_code: SH-210
legacy_name: unquoted-grep-regex
new_category: Correctness
new_code: C084
runtime_kind: ast
shellcheck_code: SC2062
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: A grep regex contains characters that may be glob-expanded.
rationale: Quote the regex argument to prevent shell expansion.
safe_fix: false
fix_description: "Quote the entire reported grep pattern word."
source:
  shell_checks_rule: rules/SH-210.yaml
  shell_checks_example: examples/210.sh
examples:
  - kind: invalid
    source: shell-checks/examples/210.sh
    code: |
      #!/bin/bash
      echo test | grep -Eq item,[0-4]
