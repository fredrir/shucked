legacy_code: SH-295
legacy_name: cd-without-error-check-in-func
new_category: Correctness
new_code: C125
runtime_kind: ast
shellcheck_code: SC2103
shellcheck_level: info
shells:
  - sh
  - bash
  - dash
  - ksh
description: "Code changes back with `cd` instead of isolating the directory change."
rationale: "Run directory-sensitive work in a subshell so the caller's working directory is restored automatically."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-295.yaml
  shell_checks_example: examples/295.sh
examples:
  - kind: invalid
    source: shell-checks/examples/295.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2164
      build() {
          cd metadata
          ./configure
          make
          cd ..
      }
