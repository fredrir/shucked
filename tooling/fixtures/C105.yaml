legacy_code: SH-239
legacy_name: export-with-positional-params
new_category: Correctness
new_code: C105
runtime_kind: ast
shellcheck_code: SC2163
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: "The `export` command receives a parameter expansion instead of a direct variable name."
rationale: Pass variable names to `export` directly instead of expanding another parameter first.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-239.yaml
  shell_checks_example: examples/239.sh
examples:
  - kind: invalid
    source: shell-checks/examples/239.sh
    code: |
      #!/bin/bash
      name=DISPLAY
      export "$name"
      export "$@"
