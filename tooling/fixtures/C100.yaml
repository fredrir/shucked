legacy_code: SH-232
legacy_name: quoted-bash-source
new_category: Correctness
new_code: C100
runtime_kind: ast
shellcheck_code: SC2128
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: "An array is referenced without an explicit element or all-elements selector."
rationale: "Use an indexed form such as `${name[0]}` for one element, or `${name[@]}` / `${name[*]}` when all elements are intended."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-232.yaml
  shell_checks_example: examples/232.sh
examples:
  - kind: invalid
    source: shell-checks/examples/232.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2034
      dir="$(dirname "$BASH_SOURCE")"
  - kind: invalid
    source: custom
    code: |
      #!/bin/bash
      paths=(src test)
      printf '%s\n' "$paths"
