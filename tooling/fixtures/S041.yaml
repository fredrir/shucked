legacy_code: SH-181
legacy_name: function-body-without-braces
new_category: Style
new_code: S041
runtime_kind: ast
shellcheck_code: SC1064
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: A function body uses an unbraced compound command that should be brace-wrapped.
rationale: "Wrap these function bodies in `{ ... }` for compatibility; subshell and arithmetic function bodies are accepted."
safe_fix: true
fix_description: "Wrap the existing compound function body in `{ ...; }` without changing the inner command."
source:
  shell_checks_rule: rules/SH-181.yaml
  shell_checks_example: examples/181.sh
examples:
  - kind: invalid
    source: shell-checks/examples/181.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2034,2154,1072,1073
      myfunc() [[ -n "$x" ]]
