legacy_code: SH-293
legacy_name: unreachable-after-exit
new_category: Correctness
new_code: C124
runtime_kind: ast
shellcheck_code: SC2317
shellcheck_level: info
shells:
  - sh
  - bash
  - dash
  - ksh
description: Code appears after all branches exit, making it unreachable.
rationale: Remove the dead code or restructure the control flow.
safe_fix: true
fix_description: "Delete the unreachable statement or definition."
source:
  shell_checks_rule: rules/SH-293.yaml
  shell_checks_example: examples/293.sh
examples:
  - kind: invalid
    source: shell-checks/examples/293.sh
    code: |
      #!/bin/bash
      if [ -f /etc/hosts ]; then
        echo found
        exit 0
      else
        echo missing
        exit 1
      fi
      echo unreachable
