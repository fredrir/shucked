legacy_code: SH-029
legacy_name: pipefail-option
new_category: Portability
new_code: X021
runtime_kind: ast
shellcheck_code: SC3040
shellcheck_level: warning
shells:
  - sh
description: The script uses a `set -o` option name that portable sh does not define.
rationale: Use only POSIX-defined `set -o` option names in sh, or switch to a shell that supports the requested mode.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-029.yaml
  shell_checks_example: examples/029.sh
examples:
  - kind: invalid
    source: shell-checks/examples/029.sh
    code: |
      #!/bin/sh
      set -o pipefail
