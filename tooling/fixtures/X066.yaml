legacy_code: SH-273
legacy_name: pipe-stderr-in-sh
new_category: Portability
new_code: X066
runtime_kind: ast
shellcheck_code: SC3073
shellcheck_level: warning
shells:
  - sh
  - dash
description: "The `|&` pipe-stderr operator is used in POSIX sh."
rationale: "Use `2>&1 |` for portable stderr piping."
safe_fix: true
fix_description: "Expand each `|&` operator to `2>&1 |`."
source:
  shell_checks_rule: rules/SH-273.yaml
  shell_checks_example: examples/273.sh
examples:
  - kind: invalid
    source: shell-checks/examples/273.sh
    code: |
      #!/bin/sh
      # shellcheck disable=2154
      echo test |& grep -q foo
