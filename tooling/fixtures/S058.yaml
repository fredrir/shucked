legacy_code: SH-240
legacy_name: unquoted-path-in-mkdir
new_category: Style
new_code: S058
runtime_kind: ast
shellcheck_code: null
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: A variable path is not quoted in a directory creation command.
rationale: Quote the variable to handle paths with spaces.
safe_fix: false
fix_description: "Wrap each reported unquoted path expansion in double quotes."
source:
  shell_checks_rule: rules/SH-240.yaml
  shell_checks_example: examples/240.sh
examples:
  - kind: invalid
    source: shell-checks/examples/240.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2086,2154
      mkdir -p -m 750 $PKG/var/lib/app
