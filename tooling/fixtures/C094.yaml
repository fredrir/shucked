legacy_code: SH-222
legacy_name: redirect-clobbers-input
new_category: Correctness
new_code: C094
runtime_kind: ast
shellcheck_code: SC2094
shellcheck_level: info
shells:
  - sh
  - bash
  - dash
  - ksh
description: A command reads and writes the same file via redirection.
rationale: "Use a temporary file or `sponge` to avoid truncating the input."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-222.yaml
  shell_checks_example: examples/222.sh
examples:
  - kind: invalid
    source: shell-checks/examples/222.sh
    code: |
      #!/bin/bash
      unzip -p "$1" test.c > test.c
