legacy_code: SH-219
legacy_name: tilde-in-string-comparison
new_category: Correctness
new_code: C091
runtime_kind: ast
shellcheck_code: SC2088
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: A quoted home-relative path keeps the tilde literal instead of expanding it.
rationale: "Expand the tilde outside quotes or use `$HOME`."
safe_fix: false
fix_description: "Rewrite the reported quoted `~/...` word as a double-quoted `$HOME/...` word."
source:
  shell_checks_rule: rules/SH-219.yaml
  shell_checks_example: examples/219.sh
examples:
  - kind: invalid
    source: shell-checks/examples/219.sh
    code: |
      #!/bin/sh
      # shellcheck disable=2154
      profile="~/.bashrc"
