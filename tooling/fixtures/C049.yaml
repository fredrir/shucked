legacy_code: SH-143
legacy_name: tautology-chain
new_category: Correctness
new_code: C049
runtime_kind: ast
shellcheck_code: SC2252
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: "The `||` chain can never fail."
rationale: "Use the matching positive test or a `case`."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-143.yaml
  shell_checks_example: examples/143.sh
examples:
  - kind: invalid
    source: shell-checks/examples/143.sh
    code: |
      #!/bin/bash
      x=a
      [[ a != "$x" ]] || [[ b != "$x" ]] || :
