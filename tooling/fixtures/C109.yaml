legacy_code: SH-244
legacy_name: mapfile-process-substitution
new_category: Correctness
new_code: C109
runtime_kind: ast
shellcheck_code: SC2339
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: "Reserved for historical SC2339 compatibility; current ShellCheck accepts `mapfile` and `readarray` reading from process substitution."
rationale: "The `mapfile -t var < <(cmd)` idiom keeps the builtin in the current shell, so it avoids the pipeline subshell pitfall. Shuck keeps C109 reserved for compatibility but does not report this pattern."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-244.yaml
  shell_checks_example: examples/244.sh
examples:
  - kind: valid
    source: shell-checks/examples/244.sh
    code: "#!/bin/bash\nmapfile -t files < <(find -name \"*.pyc\" -printf '%f\\n')\necho \"${files[@]}\"\n"
