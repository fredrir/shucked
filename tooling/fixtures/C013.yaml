legacy_code: SH-049
legacy_name: find-output-loop
new_category: Correctness
new_code: C013
runtime_kind: ast
shellcheck_code: SC2044
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: "Expanding `find` output in a `for` loop loses pathname boundaries and re-splits file names."
rationale: "Feed `find` results to a consumer that understands line or NUL boundaries instead of shell word splitting."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-049.yaml
  shell_checks_example: examples/049.sh
examples:
  - kind: invalid
    source: shell-checks/examples/049.sh
    code: |
      #!/bin/sh
      for f in $(find . -name '*.txt'); do printf '%s\n' "$f"; done
