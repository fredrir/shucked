legacy_code: SH-145
legacy_name: duplicate-redirect
new_category: Correctness
new_code: C051
runtime_kind: ast
shellcheck_code: SC2261
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: A later redirect makes an earlier one pointless.
rationale: Keep only the final redirect.
safe_fix: false
fix_description: "Delete the earlier redirect that is overridden by the later redirect."
source:
  shell_checks_rule: rules/SH-145.yaml
  shell_checks_example: examples/145.sh
examples:
  - kind: invalid
    source: shell-checks/examples/145.sh
    code: |
      #!/bin/bash
      : >/dev/null >/dev/null
