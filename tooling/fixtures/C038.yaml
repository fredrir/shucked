legacy_code: SH-112
legacy_name: else-if
new_category: Correctness
new_code: C038
runtime_kind: ast
shellcheck_code: SC1075
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: "An `if` starts on the same line as `else` instead of using `elif`."
rationale: "Collapse the nested form into a single `elif` branch."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-112.yaml
  shell_checks_example: examples/112.sh
examples:
  - kind: invalid
    source: shell-checks/examples/112.sh
    code: |
      #!/bin/bash
      if true; then
        :
      else if true; then
        :
      fi
      fi
