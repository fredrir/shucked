legacy_code: SH-033
legacy_name: replacement-expansion
new_category: Portability
new_code: X025
runtime_kind: ast
shellcheck_code: SC3060
shellcheck_level: warning
shells:
  - sh
  - dash
description: The script performs parameter replacement with a bash-only expansion form.
rationale: Use a different transformation path, or switch to bash for this replacement syntax.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-033.yaml
  shell_checks_example: examples/033.sh
examples:
  - kind: invalid
    source: shell-checks/examples/033.sh
    code: |
      #!/bin/sh
      set -- abc
      printf '%s\n' "${1//a/b}"
