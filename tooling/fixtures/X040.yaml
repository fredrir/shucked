legacy_code: SH-126
legacy_name: array-subscript-test
new_category: Portability
new_code: X040
runtime_kind: ast
shellcheck_code: SC2202
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: Uses a glob operand in a POSIX shell `[ ... ]` test
rationale: In `[ ... ]`, unquoted glob operands are not matched the way shell users usually expect. Quote literal brackets or rewrite the check as a loop, `case`, or another form that actually performs matching.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-126.yaml
  shell_checks_example: examples/126.sh
examples:
  - kind: invalid
    source: shell-checks/examples/126.sh
    code: |
      #!/bin/sh
      if [ $tools[kops] ]; then
        :
      fi
