legacy_code: SH-211
legacy_name: stderr-before-stdout-redirect
new_category: Correctness
new_code: C085
runtime_kind: ast
shellcheck_code: SC2069
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: Standard error is redirected before standard output is redirected.
rationale: "Redirect stdout first, then stderr, or use `&>`."
safe_fix: false
fix_description: "Move the reported `2>&1` redirect to after the command's stdout-to-file redirects."
source:
  shell_checks_rule: rules/SH-211.yaml
  shell_checks_example: examples/211.sh
examples:
  - kind: invalid
    source: shell-checks/examples/211.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2154
      if ! grep ^"$user": /etc/passwd 2>&1 > /dev/null; then echo missing; fi
