legacy_code: SH-284
legacy_name: malformed-arithmetic-in-condition
new_category: Correctness
new_code: C118
runtime_kind: ast
shellcheck_code: SC1076
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: An arithmetic expansion inside a conditional test is malformed.
rationale: "Close the `[[ ]]` properly and use `$(( ))` correctly."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-284.yaml
  shell_checks_example: examples/284.sh
examples:
  - kind: invalid
    source: shell-checks/examples/284.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2154,2160
      arr=(a b c d)
      if [[ true ]] && $(( ${#arr[@]}%2 )) -eq 0 ]]; then echo even; fi
