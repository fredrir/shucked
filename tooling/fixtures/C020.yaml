legacy_code: SH-073
legacy_name: truthy-literal-test
new_category: Correctness
new_code: C020
runtime_kind: ast
shellcheck_code: SC2078
shellcheck_level: style
shells:
  - sh
  - bash
  - dash
  - ksh
description: A one-word bracket or conditional test on a fixed literal does not ask a runtime question and is always decided the same way.
rationale: Check a variable, path, or explicit comparison instead of a bare constant inside `[ ... ]` or `[[ ... ]]`.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-073.yaml
  shell_checks_example: examples/073.sh
examples:
  - kind: invalid
    source: shell-checks/examples/073.sh
    code: |
      #!/bin/sh
      [ 1 ]
