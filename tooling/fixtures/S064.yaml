legacy_code: SH-255
legacy_name: xargs-with-inline-replace
new_category: Style
new_code: S064
runtime_kind: ast
shellcheck_code: SC2267
shellcheck_level: info
shells:
  - sh
  - bash
  - dash
  - ksh
description: "The `-i` flag for `xargs` is deprecated."
rationale: "Use `-I{}` instead of `-i` with `xargs`."
safe_fix: true
fix_description: "Rewrite deprecated `xargs -i` forms to the equivalent `xargs -I...` form, preserving any inline replacement string and using `{}` for bare `-i`."
source:
  shell_checks_rule: rules/SH-255.yaml
  shell_checks_example: examples/255.sh
examples:
  - kind: invalid
    source: shell-checks/examples/255.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2038
      find . -type d -name CVS | xargs -iX rm -rf "X"
