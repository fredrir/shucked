legacy_code: SH-124
legacy_name: zsh-brace-if
new_category: Portability
new_code: X038
runtime_kind: ast
shellcheck_code: SC1140
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: Zsh-style conditional bracing is not portable shell syntax.
rationale: "Use standard `if ...; then ...; fi` structure instead."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-124.yaml
  shell_checks_example: examples/124.sh
examples:
  - kind: invalid
    source: shell-checks/examples/124.sh
    code: |
      #!/bin/sh
      if [[ -n "$x" ]] {
        :
      }
