legacy_code: SH-020
legacy_name: let-command
new_category: Portability
new_code: X015
runtime_kind: ast
shellcheck_code: SC3042
shellcheck_level: warning
shells:
  - sh
  - dash
description: The script relies on `let`, which portable `sh` does not define.
rationale: Prefer arithmetic expansion or another portable arithmetic mechanism instead.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-020.yaml
  shell_checks_example: examples/020.sh
examples:
  - kind: invalid
    source: shell-checks/examples/020.sh
    code: |
      #!/bin/sh
      let x=1
      printf '%s\n' "$x"
