legacy_code: SH-199
legacy_name: ls-in-substitution
new_category: Style
new_code: S047
runtime_kind: ast
shellcheck_code: SC2012
shellcheck_level: info
shells:
  - sh
  - bash
  - dash
  - ksh
description: A pipeline processes ls output.
rationale: Use a glob or find to avoid parsing ls output.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-199.yaml
  shell_checks_example: examples/199.sh
examples:
  - kind: invalid
    source: shell-checks/examples/199.sh
    code: |
      #!/bin/bash
      LAYOUTS="$(ls layout.*.h | cut -d. -f2 | xargs echo)"
      echo "$LAYOUTS"
