legacy_code: SH-292
legacy_name: function-references-unset-param
new_category: Correctness
new_code: C123
runtime_kind: ast
shellcheck_code: SC2119
shellcheck_level: info
shells:
  - sh
  - bash
  - dash
  - ksh
  - zsh
description: A function references a positional parameter that is never passed by callers.
rationale: Pass the expected arguments at every call site.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-292.yaml
  shell_checks_example: examples/292.sh
examples:
  - kind: invalid
    source: shell-checks/examples/292.sh
    code: |
      #!/bin/sh
      greet() { echo "hello $1"; }
      greet
