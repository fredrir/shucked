legacy_code: SH-015
legacy_name: brace-expansion
new_category: Portability
new_code: X010
runtime_kind: ast
shellcheck_code: SC3009
shellcheck_level: warning
shells:
  - sh
  - dash
description: The script depends on brace expansion, which portable `sh` does not expand.
rationale: Spell out the values explicitly or generate them in a loop instead of using braces.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-015.yaml
  shell_checks_example: examples/015.sh
examples:
  - kind: invalid
    source: shell-checks/examples/015.sh
    code: |
      #!/bin/sh
      echo {a,b}
