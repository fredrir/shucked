legacy_code: SH-327
legacy_name: dangling-else
new_category: Correctness
new_code: C143
runtime_kind: ast
shellcheck_code: SC2391
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: Else branch has no body
rationale: Put a command before the final fi.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-327.yaml
  shell_checks_example: examples/327.sh
examples:
  - kind: invalid
    source: shell-checks/examples/327.sh
    code: |
      #!/bin/sh
      if true; then echo yes; else fi
