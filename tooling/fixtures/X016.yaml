legacy_code: SH-021
legacy_name: declare-command
new_category: Portability
new_code: X016
runtime_kind: ast
shellcheck_code: SC3044
shellcheck_level: warning
shells:
  - sh
  - dash
description: The script uses a shell builtin that portable `sh` does not define.
rationale: Replace the builtin with a portable construct, or require a shell that provides it intentionally.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-021.yaml
  shell_checks_example: examples/021.sh
examples:
  - kind: invalid
    source: shell-checks/examples/021.sh
    code: |
      #!/bin/sh
      declare
  - kind: invalid
    code: |
      #!/bin/sh
      typeset value=1
  - kind: invalid
    code: |
      #!/bin/sh
      shopt -s nullglob
  - kind: invalid
    code: |
      #!/bin/sh
      mapfile entries
  - kind: invalid
    code: |
      #!/bin/sh
      pushd /tmp
