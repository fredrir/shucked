legacy_code: SH-174
legacy_name: extglob-in-test
new_category: Portability
new_code: X046
runtime_kind: ast
shellcheck_code: SC1036
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: An extended glob pattern appears inside a test expression.
rationale: Rewrite the condition with a portable pattern or a `case` statement.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-174.yaml
  shell_checks_example: examples/174.sh
examples:
  - kind: invalid
    source: shell-checks/examples/101.sh
    code: |
      #!/bin/sh
      if [[ $term == @(xterm|screen)* ]]; then
        :
      fi
  - kind: invalid
    source: shell-checks/examples/174.sh
    code: |
      #!/bin/sh
      [ "$x" = (foo|bar)* ]
