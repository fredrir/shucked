legacy_code: SH-034
legacy_name: legacy-backticks
new_category: Style
new_code: S005
runtime_kind: ast
shellcheck_code: SC2006
shellcheck_level: style
shells:
  - sh
  - bash
  - dash
  - ksh
description: Backtick command substitution is a legacy form that is harder to nest and quote cleanly.
rationale: "Use `$(...)` instead of backticks so substitutions stay readable and composable."
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-034.yaml
  shell_checks_example: examples/034.sh
examples:
  - kind: invalid
    source: shell-checks/examples/034.sh
    code: |
      #!/bin/bash
      x=`date`
      echo "$x"
