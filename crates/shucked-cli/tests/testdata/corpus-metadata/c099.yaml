reviewed_divergences:
  - side: shellcheck-only
    path_contains: "void-linux__void-packages__common__scripts__lint-conflicts"
    line: 168
    reason: "This assignment stores the `${array[@]@A}` declaration-style representation so it can be evaluated later; the scalar value is an intentional serialization, not an accidental loss of array element boundaries."
