#!/bin/bash
echo "$PIDFILE exists, skipping "$INTERVAL" run" >> "$LOGFILE"
echo ""$PIDFILE""
echo "$a"$b"$c"
echo "left "$b""
echo ""$b" right"
echo "left "middle" right"
echo prefix"$value"suffix
echo "$a"$@"$c"
echo "$a"$(printf '%s' hi)"$c"
echo "$a"${items[@]}"$c"
