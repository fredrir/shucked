#!/bin/sh

# Invalid: `fi` has no matching `if`.
fi

# Invalid: `done` has no matching loop opener.
done

# Invalid: `esac` has no matching `case`.
esac

# Invalid: `then` has no matching `if`.
then

# Invalid: `else` has no matching `if`.
else

# Invalid: `elif` has no matching `if`.
elif

# Invalid: `do` has no matching loop header.
do
