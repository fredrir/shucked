---
source: crates/shuck-linter/src/rules/style/ifs_equals_ambiguity.rs
assertion_line: 181
---
Diagnostics:
S042 quote `=` when assigning IFS to avoid looking like `==`
 --> <source>:2:11
  |
2 | while IFS== read -r key val; do
  |


Applied fixes: 1

Diff:
--- before
+++ after
@@ -1,4 +1,4 @@
 #!/bin/bash
-while IFS== read -r key val; do
+while IFS='=' read -r key val; do
   echo "$key=$val"
 done < /dev/null

Fixed source:
#!/bin/bash
while IFS='=' read -r key val; do
  echo "$key=$val"
done < /dev/null

Remaining diagnostics:
<none>
