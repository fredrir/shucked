---
source: crates/shuck-linter/src/rules/portability/conditional_portability.rs
---
Diagnostics:
X060 `-v` tests are not available in POSIX sh
 --> <source>:2:4
  |
2 | [[ -v myvar ]] || :
  |


Applied fixes: 1

Diff:
--- before
+++ after
@@ -1,3 +1,3 @@
 #!/bin/sh
-[[ -v myvar ]] || :
+[ -n "${myvar+set}" ] || :
 [[ -n ${myvar+set} ]] || :

Fixed source:
#!/bin/sh
[ -n "${myvar+set}" ] || :
[[ -n ${myvar+set} ]] || :

Remaining diagnostics:
<none>
